import 'package:flutter/material.dart';

class AnimalHospital extends StatelessWidget {
  const AnimalHospital({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Animal Hospital Screen', style: TextStyle(fontSize: 24)),
    );
  }
}